#!/usr/bin/env bash

set -e

VENV=.venv/patch_elf_float_abi

make -C $REPO_ROOT/bin venv VENV=$VENV FILE=$REPO_ROOT/sw/scripts/patch_elf_float_abi-requirements.txt > /dev/null 2>&1

source $REPO_ROOT/bin/$VENV/bin/activate
$REPO_ROOT/sw/scripts/patch_elf_float_abi.py $@
deactivate
