#!/usr/bin/env bash

set -e

VENV=.venv/texthexify

make -C $REPO_ROOT/bin venv VENV=$VENV FILE=$REPO_ROOT/sw/scripts/texthexify/requirements.txt > /dev/null 2>&1

source $REPO_ROOT/bin/$VENV/bin/activate
$REPO_ROOT/sw/scripts/texthexify/texthexify.py $@
deactivate
