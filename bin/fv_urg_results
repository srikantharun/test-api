#!/usr/bin/env bash

set -e

VENV=.venv/fv_urg_results

make -C $REPO_ROOT/bin venv VENV=$VENV FILE=$REPO_ROOT/hw/scripts/fv/fv-requirements.txt > /dev/null 2>&1

source $REPO_ROOT/bin/$VENV/bin/activate
$REPO_ROOT/hw/scripts/fv/fv_urg_results.py $@
deactivate
