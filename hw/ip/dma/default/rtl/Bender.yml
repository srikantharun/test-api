# (C) Copyright 2024 Axelera AI B.V.
# All Rights Reserved
# *** Axelera AI Confidential ***

package:
  name: dma
  description: "TODO: add description"
  authors:
    - "Matt Morris <matt.morris@axelera.ai>"

dependencies:
  axi_pkg: { path: "hw/ip/axi/default/rtl/pkg" }
  dma_pkg: { path: "pkg" }
  cmdblock: { path: "hw/ip/cmdblock/default/rtl" }
  axe_tcl: { path: "hw/ip/tech_cell_library/default/rtl" }
  lowrisc_prim_subreg: { path: hw/vendor/lowrisc/prim_subreg/default/rtl/" }
  axi4_common: { path: "hw/ip/axi4_common/default/rtl/" }
  aic_fabric_addr_decoder: { path: "" }

# Source files grouped in levels.
# Files in level 0 have no dependencies on files in this package.
# Files in level 1 only depend on files in level 0, files in level 2 on files in levels 1 and 0, etc.
# Files within a level are ordered alphabetically.
sources:
  - target: rtl
    files:
      # Level 0
      - build_reg/dma_reg_top.sv
      - build_reg/dma_channel_reg_top.sv
      - build_reg/dma_mmu_reg_top.sv
      - dma_coalesce.sv
      - dma_events.sv
      - dma_channel_ll.sv
      - dma_channel_ctrl.sv
      - dma_pipeline_ctrl.sv
      - dma_channel_src.sv
      - dma_fifo_logic.sv
      - dma_channel_buffer.sv
      - dma_channel_dst.sv
      - dma_channel.sv
      - dma_progif.sv
      - dma_port_arb.sv
      - dma_mmu.sv
      - dma_axi_rd_port_mux.sv
      - dma_axi_wr_port_mux.sv
      - dma.sv
