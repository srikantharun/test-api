package:
  name: dma_4channel_tb 
  description: "DMA 4 channel Testbench"
  authors:
    - "andrew.bond@axelera.ai"

dependencies:
  dv_axe_dma:   { path: "$REPO_ROOT/hw/dv/axe_dma" }

sources:
  - target: simulation
    include_dirs:
      - ./rtl
      - ./uvm
    files:
      - ../uvm/test_pkg.sv
      - ../rtl/tb.sv
