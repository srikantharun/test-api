{% macro render_items(items,detailed)-%}

{% if detailed:-%}
# Europa Detailed Memory Map
{% else:-%}
# Europa Memory Map
{% endif-%}

### !!! AUTOGENERATED FILE- PLEASE DO NOT UPDATE MANUALLY!!!
This file is autogenerated from the gold reference of the memory map in `hw/impl/europa/data/memory_map/memory_map.yml`

| Block      | Start Address | End Address   | Region | R   | W   | X   | Description        | Size      | NoC Addressing Scheme  |
|------------|---------------|---------------|--------|-----|-----|-----|--------------------|-----------|------------------------|
{% for item in items -%}
{%- set rd_flag = 'x' if item.flags and 'R' in item.flags else '' -%}
{%- set wr_flag = 'x' if item.flags and 'W' in item.flags else '' -%}
{%- set ex_flag = 'x' if item.flags and 'X' in item.flags else '' -%}
{%- set ip_name = (item.block ~ '_' ~ item.name) | upper if item.name != item.block else item.name | upper -%}
| {{ ip_name }} | 0x{{ "{:08X}".format(item.offset) }} | 0x{{ "{:08X}".format(item.offset + item.size - 1) }} | {{ item.type }} | {{ rd_flag }} | {{ wr_flag }} | {{ ex_flag }} | {{ item.description or '' }} | {{ item.size // 1024 }} KiB | {{ item.noc_addr_scheme or '' }} |
{% endfor %}

{% endmacro %}
{{ render_items(items, detailed) }}







