#%Module1.0
##
## Modulefile for mybinary
##
proc ModulesHelp { } {
    puts stderr "Adds the REPO_ROOT/bin folder to the path."
}

module-whatis "Loads REPO_ROOT/bin into the environment"

prepend-path PATH [getenv REPO_ROOT]/bin
