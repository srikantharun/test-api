##### LABELS
## HW_IP_TKN_MNG_DV_NIGHTLY
# -tkn_mng_loc_dev_test

## HW_IP_TKN_MNG_DV_MERGE_REQUEST
# -tkn_mng_loc_dev_test


tests:
  - name: tkn_mng_hello_world_test
    description: "Just some compilation sanity"
    requirement_ids: []
    labels: [HW_IP_TKN_MNG_DV_MERGE_REQUEST]
    platforms: [uvm.HW_IP_TKN_MNG_DEFAULT_DV_SIM]
    flags: [--uvm_testname=tkn_mng_hello_world_test]
    seeds: [1]
    owner: Vito Luca Guglielmi

tests:
  - name: tkn_mng_loc_dev_test
    description: "The test is producing anc sonuming tokens on the local network and checking the values of the counters in the middle and at the end."
    requirement_ids: []
    labels: [HW_IP_TKN_MNG_DV_NIGHTLY, HW_IP_TKN_MNG_DV_MERGE_REQUEST]
    platforms: [uvm.HW_IP_TKN_MNG_DEFAULT_DV_SIM]
    flags: [--uvm_testname=tkn_mng_loc_dev_test]
    seeds: [1]
    repeat_count: 5
    owner: Vito Luca Guglielmi
